---
import SkipNavContent from '@components/a11y/skip-nav-content.astro'
import Seo from '@components/seo.astro'
import TideDialogImageViewer from '@components/TideDialogImageViewer.astro'
import TideImageMasonry from '@components/TideImageMasonry.astro'
import TideMasonryItem from '@components/TideMasonryItem.astro'
import TidePicture from '@components/TidePicture.astro'
import { LOCALES } from '@constants'
import Layout from '@layouts/layout.astro'
import type { GetStaticPaths } from 'astro'
import myImage from 'D:\\Users\\Hollis\\Desktop\\astro-cap.jt5HlOg8_1PlEan.webp'

export const getStaticPaths = (async () => {
	return LOCALES.map((locale) => ({
		params: { locale },
	}))
}) satisfies GetStaticPaths
---

<Layout>
	<Seo slot="seo" />
	<SkipNavContent>
		<TideImageMasonry>
			<TideMasonryItem
				class="bg-amber-500"
				imgClass="bg-blue-500"
				src={myImage}
				formats={['avif', 'webp']}
				alt="A description of my image."
			/>
			<TideMasonryItem
				class="bg-amber-500"
				src={myImage}
				inferSize
				formats={['avif', 'webp']}
				alt="A description of my image."
			/>
			<TideMasonryItem
				mask="aaa"
				class="bg-amber-500"
				src={myImage}
				inferSize
				formats={['avif', 'webp']}
				alt="A description of my image."
			/>
			<TideMasonryItem
				class="bg-amber-500"
				src={myImage}
				inferSize
				formats={['avif', 'webp']}
				alt="A description of my image."
			/>
		</TideImageMasonry>
		<TideImageMasonry>
			<TideMasonryItem class="bg-amber-500" src={myImage} formats={['avif', 'webp']} alt="A description of my image." />
			<TideMasonryItem
				mask=""
				class="bg-amber-500"
				src={myImage}
				inferSize
				formats={['avif', 'webp']}
				alt="A description of my image."
			/>
			<TideMasonryItem
				title="《我的世界》"
				caption="《我的世界》中由程序生成的地形的一个示例，图中包含的元素有玩家史蒂夫，平原、森林、高山生物群系和一个村庄"
				src={myImage}
				inferSize
				formats={['avif', 'webp']}
				alt="A description of my image."
			/>
			<TideMasonryItem
				class="bg-amber-500"
				src={myImage}
				inferSize
				formats={['avif', 'webp']}
				alt="A description of my image."
			/>
			<TideMasonryItem
				intent="inline"
				class="bg-amber-500"
				src={myImage}
				inferSize
				formats={['avif', 'webp']}
				alt="A description of my image."
			/>
		</TideImageMasonry>
		<TidePicture
			src={myImage}
			inferSize
			formats={['avif', 'webp']}
			alt="A description of my image."
			title="only title"
		/>
		<TidePicture
			src={myImage}
			inferSize
			formats={['avif', 'webp']}
			alt="A description of my image."
			caption="only caption"
		/>
		<TidePicture
			mask="aaa"
			src={myImage}
			inferSize
			formats={['avif', 'webp']}
			alt="A description of my image."
			title="title"
			caption="caption"
		/>
		<div class="w-full bg-purple-500">
			<TidePicture
				intent="inline"
				class="bg-green-500"
				src={myImage}
				inferSize
				formats={['avif', 'webp']}
				alt="A description of my image."
				title="title"
				caption="caption"
			/>
		</div>
	</SkipNavContent>
</Layout>
