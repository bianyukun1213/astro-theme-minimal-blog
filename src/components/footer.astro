---
import { removeTrailingSlash, useTranslations, type Locale } from '@utils'
import Link from './link.astro'
import Spacer from './spacer.astro'
import ThemeSelect from './theme-select.astro'

const currentLocale = Astro.currentLocale as Locale
const m = useTranslations(currentLocale)

const base = import.meta.env.BASE_URL
---

<footer class="container">
	<Spacer axis="vertical" size={32} />
	<div class="flex flex-row justify-between items-center pt-4 border-t border-border">
		<div class="flex gap-4">
			<ThemeSelect />
			<span class="w-px ht-auto bg-border"></span>
			<Link href={`${removeTrailingSlash(base)}/${currentLocale}/rss.xml`}>{m.btn_rss_feed_title()}</Link>
		</div>
		<div>
			<Link href="http://github.com/LekoArts/astro-theme-minimal-blog">Theme</Link> by <Link
				href="https://www.lekoarts.de/?utm_source=astro-theme-minimal-blog">LekoArts</Link
			>
		</div>
	</div>
	<Spacer axis="vertical" size={8} />
</footer>
